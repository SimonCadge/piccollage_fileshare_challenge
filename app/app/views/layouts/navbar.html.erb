<nav class="navbar navbar-expand-sm navbar-light p-2 m-2 rounded secondary">
    <a class="navbar-brand" href=<%= welcome_index_path %>>
        <%= image_tag("file-share-icon.png", width: "30", height: "30")%>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item">
                <%= link_to "Home", welcome_index_path, class: current_page?(welcome_index_path) || current_page?(session_path) ? "nav-link active" : "nav-link" %>
            </li>
            <% if is_logged_in %>
                <li class="nav-item">
                    <%= link_to current_user.email, user_path(current_user), class: current_page?(user_path(current_user)) ? "nav-link active" : "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= button_to "Logout", session_path, method: :delete, class: "btn" %>
                </li>
            <% end %>
        </ul>
    </div>
</nav>