<%= form_with(model: shared_file) do |form| %>
  <% if shared_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shared_file.errors.count, "error") %> prohibited this shared_file from being saved:</h2>

      <ul>
        <% shared_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :attached_file, style: "display: block" %>
    <%= form.file_field :attached_file %>
  </div>

  <div>
    <%= form.label :link_type, style: "display: block" %>
    <%= form.select :link_type, ['short', 'long', 'forever'] %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
